# Security Policy

Do not open public issues for suspected vulnerabilities. Contact the repository maintainer
privately with reproduction steps, affected versions, and impact. Do not include real OAuth
tokens, private calendar data, or sensitive saved URLs.

Supported security fixes target the latest release. Lydra uses no remote code, validates
external data, minimizes permissions, and keeps Calendar operations behind explicit user
confirmation.
