# Contributing

Keep changes small, tested, and scoped to an issue. Before submitting a change, run
`pnpm check`. Changes to OAuth scopes, browser permissions, external services, telemetry,
or event-confirmation behavior require an ADR and maintainer approval.

Never commit credentials, private calendar data, signing material, or sensitive URLs.
