name: Release candidate

on:
  workflow_dispatch:
  push:
    tags: ["v*"]

permissions:
  contents: read

jobs:
  package:
    runs-on: ubuntu-latest
    environment: release
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm check
      - run: pnpm package
        env:
          LYDRA_GOOGLE_OAUTH_CLIENT_ID: ${{ secrets.LYDRA_GOOGLE_OAUTH_CLIENT_ID }}
      - uses: actions/upload-artifact@v6
        with:
          name: lydra-release-candidate
          path: release/
          if-no-files-found: error
